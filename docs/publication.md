# Publication status

This fork is not published to npm.

`package.json` is marked `private` and uses the non-release version `0.0.0-private`. Install the extension from `github.com/Whamp/pi-observational-memory` or a local checkout.

A future publication change must separately decide the package name, release version, registry, ownership, credentials, and release process. This integration does not make those decisions and must not publish a package or create a release.
